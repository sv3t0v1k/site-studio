# 🚀 Noir Portfolio - Полное руководство по развертыванию

## Обзор архитектуры

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Cursor IDE    │───▶│    GitHub       │───▶│  Docker Server  │
│   (Разработка)  │    │   Repository    │    │   (Продакшн)   │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         ▼                       ▼                       ▼
   Локальные изменения    GitHub Actions         Автоматическое
   и тестирование         CI/CD Pipeline         обновление
```

## 📋 Предварительные требования

### На вашем компьютере (для разработки):
- **Node.js 18+** - [nodejs.org](https://nodejs.org/)
- **Git** - [git-scm.com](https://git-scm.com/)
- **Cursor IDE** - [cursor.sh](https://cursor.sh/)

### На сервере:
- **Ubuntu/Debian** сервер (или другой Linux)
- **Docker** и **Docker Compose**
- **Nginx** (для проксирования)
- **Git**
- **SSH доступ**

---

## 🚀 Часть 1: Настройка GitHub репозитория

### Шаг 1.1: Создание репозитория

1. Перейдите на [github.com](https://github.com)
2. Нажмите **"New repository"**
3. Укажите название: `noir-portfolio`
4. **НЕ** добавляйте README, .gitignore, license (мы их уже имеем)
5. Нажмите **"Create repository"**

### Шаг 1.2: Загрузка кода

```bash
# Перейдите в папку с проектом
cd noir-portfolio

# Инициализируйте git если не инициализирован
git init

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit: Noir Portfolio with Rich Text Editor"

# Добавьте remote repository
git remote add origin https://github.com/YOUR_USERNAME/noir-portfolio.git

# Отправьте код
git push -u origin main
```

### Шаг 1.3: Настройка GitHub Secrets

1. Перейдите в ваш репозиторий → **Settings** → **Secrets and variables** → **Actions**
2. Добавьте следующие секреты:

```
SERVER_HOST=your-server-ip-or-domain
SERVER_USER=your-server-username
SERVER_PORT=22
SERVER_SSH_KEY=your-private-ssh-key
```

#### Как получить SSH ключ:
```bash
# На вашем компьютере
ssh-keygen -t rsa -b 4096 -C "github-deploy@your-server"

# Скопируйте публичный ключ
cat ~/.ssh/id_rsa.pub

# Добавьте его на сервер в ~/.ssh/authorized_keys
ssh-copy-id user@your-server
```

---

## 🖥️ Часть 2: Настройка домашнего сервера

### Шаг 2.1: Базовая настройка сервера

```bash
# Подключитесь к серверу
ssh user@your-server

# Запустите скрипт настройки
wget https://raw.githubusercontent.com/YOUR_USERNAME/noir-portfolio/main/server-setup.sh
chmod +x server-setup.sh
sudo ./server-setup.sh
```

### Шаг 2.2: Настройка переменных окружения

```bash
# Перейдите в папку приложения
cd /opt/noir-portfolio

# Отредактируйте файл с переменными
nano .env.prod
```

Заполните следующие обязательные поля:

```env
# База данных
DB_USER=noir_user
DB_PASSWORD=your-secure-db-password-here

# Безопасность
JWT_SECRET=generate-secure-random-string-here

# Админ аккаунт
ADMIN_EMAIL=admin@your-domain.com
ADMIN_PASSWORD=your-secure-admin-password

# Домен (если есть)
NEXTAUTH_URL=https://your-domain.com

# GitHub (для Docker builds)
GITHUB_REPOSITORY=YOUR_USERNAME/noir-portfolio
```

### Шаг 2.3: Первый запуск

```bash
# Запустите развертывание
./deploy.sh
```

Если все прошло успешно, приложение будет доступно на порту 80 вашего сервера.

---

## 🔧 Часть 3: Настройка домена и SSL (опционально)

### Шаг 3.1: Настройка домена

```bash
# На сервере отредактируйте nginx.conf
sudo nano /opt/noir-portfolio/nginx.conf

# Измените server_name
server_name your-domain.com;

# Перезапустите nginx
docker-compose -f docker-compose.prod.yml restart nginx
```

### Шаг 3.2: SSL сертификаты

```bash
# Получите бесплатный сертификат Let's Encrypt
sudo certbot --nginx -d your-domain.com

# Или разместите свои сертификаты в папке ssl/
# cert.pem и key.pem
```

---

## 🔄 Часть 4: Рабочий процесс разработки

### 4.1: Локальная разработка

```bash
# В Cursor IDE работайте над проектом
# Тестируйте локально
npm run dev

# Когда готовы к развертыванию
git add .
git commit -m "Your changes description"
git push origin main
```

### 4.2: Автоматическое развертывание

После `git push` автоматически запустится:

1. **GitHub Actions** соберет Docker образ
2. Загрузит его в GitHub Container Registry
3. Подключится к вашему серверу по SSH
4. Обновит контейнеры

### 4.3: Мониторинг развертывания

```bash
# На сервере проверяйте статус
cd /opt/noir-portfolio
docker-compose -f docker-compose.prod.yml ps

# Просматривайте логи
docker-compose -f docker-compose.prod.yml logs -f app

# Проверяйте здоровье
curl http://localhost/health
```

---

## 🛠️ Часть 5: Управление и обслуживание

### 5.1: Резервное копирование

```bash
# На сервере создайте скрипт backup.sh
#!/bin/bash
TIMESTAMP=$(date +"%Y%m%d_%H%M%S")
docker exec noir_portfolio_db_prod pg_dump -U noir_user noir_portfolio_prod > "/opt/noir-portfolio-backups/db_$TIMESTAMP.sql"
echo "Backup created: db_$TIMESTAMP.sql"
```

### 5.2: Обновление вручную

```bash
# На сервере
cd /opt/noir-portfolio
./deploy.sh
```

### 5.3: Масштабирование

Для увеличения производительности:

```yaml
# В docker-compose.prod.yml добавьте
services:
  app:
    deploy:
      replicas: 3
    # ... другие настройки
```

### 5.4: Мониторинг

```bash
# Установите monitoring
docker run -d \
  --name prometheus \
  -p 9090:9090 \
  prom/prometheus

# Просмотр метрик
curl http://localhost:9090
```

---

## 🚨 Часть 6: Устранение неполадок

### Проблема: GitHub Actions не может подключиться к серверу

**Решение:**
```bash
# Проверьте SSH ключ
ssh -T user@your-server

# Убедитесь что ключ добавлен в authorized_keys
cat ~/.ssh/authorized_keys
```

### Проблема: Контейнеры не запускаются

**Решение:**
```bash
# Проверьте логи
docker-compose -f docker-compose.prod.yml logs

# Проверьте переменные окружения
cat .env.prod

# Перезапустите
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml up -d
```

### Проблема: Сайт недоступен

**Решение:**
```bash
# Проверьте nginx
curl http://localhost

# Проверьте приложение
curl http://localhost:3000

# Проверьте порты
sudo netstat -tlnp | grep :80
sudo netstat -tlnp | grep :3000
```

### Проблема: База данных недоступна

**Решение:**
```bash
# Проверьте статус базы данных
docker-compose -f docker-compose.prod.yml ps db

# Проверьте логи
docker-compose -f docker-compose.prod.yml logs db

# Пересоздайте базу
docker-compose -f docker-compose.prod.yml down -v
docker-compose -f docker-compose.prod.yml up -d db
```

---

## 📊 Часть 7: Производительность и оптимизация

### 7.1: Кеширование

```nginx
# В nginx.conf добавьте
location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

### 7.2: CDN

Для статических файлов используйте Cloudflare или аналог.

### 7.3: Мониторинг

```bash
# Установите monitoring stack
docker run -d --name grafana -p 3001:3000 grafana/grafana
```

---

## 🔒 Часть 8: Безопасность

### 8.1: SSH hardening

```bash
# Отключите парольную аутентификацию
sudo nano /etc/ssh/sshd_config
# PasswordAuthentication no
# PubkeyAuthentication yes

sudo systemctl restart ssh
```

### 8.2: Firewall

```bash
# Проверьте правила
sudo ufw status

# Добавьте необходимые правила
sudo ufw allow from your-ip to any port 22
```

### 8.3: SSL/TLS

```bash
# Всегда используйте HTTPS
# Перенаправляйте HTTP на HTTPS в nginx.conf
```

---

## 🎯 Заключение

Теперь у вас есть полнофункциональная система CI/CD для Noir Portfolio!

**Ключевые преимущества:**
- ✅ **Быстрая разработка** в Cursor IDE
- ✅ **Автоматическое развертывание** через GitHub Actions
- ✅ **Надежный хостинг** на вашем сервере
- ✅ **Масштабируемость** через Docker
- ✅ **Безопасность** и **мониторинг**

**Следующие шаги:**
1. Начните разработку в Cursor
2. Тестируйте локально
3. Коммитьте и пушьте изменения
4. Автоматическое развертывание запустится само
5. Проверяйте результат на вашем домене

**🚀 Удачи с развертыванием Noir Portfolio!**
