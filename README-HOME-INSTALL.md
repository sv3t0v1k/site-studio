# 🚀 Noir Portfolio - Установка в домашнюю директорию

## Почему домашняя директория лучше /opt?

### ✅ **Преимущества:**
- **Не нужны root права** для установки
- **Легче управлять** файлами и бэкапами
- **Безопаснее** - нет системных директорий
- **Удобнее для разработки** - все в одном месте
- **Легче переносить** между серверами

### 📁 **Структура в домашней директории:**

```
~/
├── noir-portfolio/           # Основной проект
│   ├── docker-compose.prod.yml
│   ├── .env.prod
│   ├── deploy.sh
│   └── ... (все файлы проекта)
│
├── noir-portfolio-backups/   # Автоматические бэкапы
│   ├── db_backup_20241210.sql
│   └── ...
│
├── .ssh/                     # SSH ключи
└── .bashrc                   # Настройки терминала
```

---

## 🎯 ШАГ 1: Настройка сервера

### Подключитесь к серверу:
```bash
ssh user@your-server
```

### Запустите настройку (без sudo!):
```bash
# Скачайте скрипт
wget https://raw.githubusercontent.com/YOUR_USERNAME/noir-portfolio/main/server-setup.sh

# Сделайте исполняемым
chmod +x server-setup.sh

# Запустите настройку (работает без root)
./server-setup.sh
```

---

## 🎯 ШАГ 2: Настройка переменных окружения

### Перейдите в папку проекта:
```bash
cd ~/noir-portfolio
```

### Отредактируйте переменные:
```bash
nano .env.prod
```

**Заполните:**
```env
# База данных
DB_USER=noir_user
DB_PASSWORD=ваш-сильный-пароль

# Безопасность
JWT_SECRET=минимум-64-символа-случайная-строка

# Админ аккаунт
ADMIN_EMAIL=admin@your-domain.com
ADMIN_PASSWORD=ваш-сильный-пароль

# Домен
NEXTAUTH_URL=https://your-domain.com

# GitHub
GITHUB_REPOSITORY=YOUR_USERNAME/noir-portfolio
```

---

## 🎯 ШАГ 3: Первый запуск

### Запустите развертывание:
```bash
# Сделайте скрипт исполняемым
chmod +x deploy.sh

# Запустите первое развертывание
./deploy.sh
```

**Что запустится:**
- 🐘 PostgreSQL база данных
- ⚛️ Next.js приложение
- 🌐 Nginx веб-сервер

---

## 🎯 ШАГ 4: Настройка SSH для GitHub (ВАЖНО!)

### **Настройте SSH доступ к GitHub:**
```bash
# Сгенерируйте SSH ключ (если нет)
ssh-keygen -t rsa -b 4096 -C "your-email@example.com"

# Добавьте публичный ключ в GitHub
cat ~/.ssh/id_rsa.pub

# Скопируйте вывод выше и добавьте в:
# GitHub → Settings → SSH and GPG keys → New SSH key

# Протестируйте соединение
ssh -T git@github.com
```

### **Результат:** Должно появиться: `Hi sv3t0v1k! You've successfully authenticated...`

---

## 🎯 ШАГ 5: Проверка работы

### Посмотрите статус:
```bash
cd ~/noir-portfolio
docker-compose -f docker-compose.prod.yml ps
```

### Посмотрите логи:
```bash
# Все сервисы
docker-compose -f docker-compose.prod.yml logs -f

# Только приложение
docker-compose -f docker-compose.prod.yml logs -f app
```

### Проверьте доступность:
```bash
# API здоровье
curl http://localhost/api/health

# Главная страница
curl http://localhost
```

---

## 🎯 ШАГ 6: Настройка домена и SSL

### Установите SSL сертификат:
```bash
# Установите certbot
sudo apt install certbot python3-certbot-nginx

# Получите сертификат
sudo certbot --nginx -d your-domain.com
```

### Перезапустите сервисы:
```bash
cd ~/noir-portfolio
docker-compose -f docker-compose.prod.yml restart nginx
```

---

## 📁 Управление проектом

### **Все команды выполняются из:**
```bash
cd ~/noir-portfolio
```

### **Запуск/остановка:**
```bash
# Запустить все
docker-compose -f docker-compose.prod.yml up -d

# Остановить все
docker-compose -f docker-compose.prod.yml down

# Перезапустить
docker-compose -f docker-compose.prod.yml restart
```

### **Логи и мониторинг:**
```bash
# Все логи
docker-compose -f docker-compose.prod.yml logs -f

# Логи базы данных
docker-compose -f docker-compose.prod.yml logs -f db

# Логи приложения
docker-compose -f docker-compose.prod.yml logs -f app
```

### **Бэкапы:**
```bash
# Ручной бэкап базы данных
docker exec noir_portfolio_db_prod pg_dump -U noir_user noir_portfolio_prod > ~/noir-portfolio-backups/manual_$(date +%Y%m%d).sql

# Просмотр бэкапов
ls -la ~/noir-portfolio-backups/
```

---

## 🔄 Автоматические обновления

### **Через GitHub Actions (рекомендуется):**
После настройки GitHub Secrets каждый `git push` автоматически:
- Собирает новый Docker образ
- Обновляет контейнеры
- Перезапускает приложение

### **Через systemd (опционально):**
```bash
# Ежедневные обновления (включено в server-setup.sh)
sudo systemctl status noir-portfolio-update.timer
```

---

## 🛠️ Устранение проблем

### **Контейнеры не запускаются:**
```bash
# Проверьте переменные
cat ~/noir-portfolio/.env.prod

# Проверьте логи
docker-compose -f docker-compose.prod.yml logs
```

### **База данных недоступна:**
```bash
# Проверьте статус
docker-compose -f docker-compose.prod.yml ps db

# Посмотрите логи БД
docker-compose -f docker-compose.prod.yml logs db
```

### **Приложение не отвечает:**
```bash
# Проверьте здоровье
curl http://localhost:3000/api/health

# Посмотрите логи приложения
docker-compose -f docker-compose.prod.yml logs app
```

---

## 📂 Структура файлов

```
~/
├── noir-portfolio/                    # Основной проект
│   ├── docker-compose.prod.yml       # Docker конфигурация
│   ├── .env.prod                     # Переменные окружения
│   ├── deploy.sh                     # Скрипт развертывания
│   ├── server-setup.sh              # Скрипт настройки сервера
│   ├── nginx.conf                    # Nginx конфигурация
│   ├── Dockerfile                    # Docker образ приложения
│   ├── src/                          # Исходный код Next.js
│   ├── prisma/                       # База данных
│   ├── .github/workflows/            # CI/CD
│   └── logs/                         # Логи приложения
│
├── noir-portfolio-backups/           # Бэкапы базы данных
│   ├── db_backup_20241210.sql
│   └── ...
│
└── .ssh/                            # SSH ключи
    ├── id_rsa                       # Приватный ключ
    └── id_rsa.pub                   # Публичный ключ
```

---

## 🎉 Готово!

### **Ваш Noir Portfolio работает:**
- 🌐 **Главная страница:** `http://your-server-ip` или `https://your-domain.com`
- 👨‍💻 **Админ-панель:** `/admin/login`
- 📧 **Email:** admin@your-domain.com
- 🔑 **Пароль:** ваш пароль из .env.prod

### **Автоматические обновления:**
- Каждый `git push` → автоматическое развертывание
- Ежедневные бэкапы → в `~/noir-portfolio-backups/`
- Мониторинг логов → через Docker logs

**Установка в домашней директории завершена!** 🚀✨
