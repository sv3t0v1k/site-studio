# Noir Portfolio - Modern Full-Stack Application

Полноценное портфолио-приложение, построенное на Next.js 14, TypeScript, Prisma и PostgreSQL.

## 🚀 Возможности

- **Современный стек**: Next.js 14, TypeScript, Tailwind CSS
- **База данных**: PostgreSQL с Prisma ORM
- **Аутентификация**: JWT токены
- **Админ-панель**: Полнофункциональная CMS
- **API**: RESTful API для всех операций
- **Email**: Отправка уведомлений
- **Адаптивный дизайн**: Mobile-first подход
- **SEO**: Оптимизировано для поисковых систем

## 🛠 Технологии

### Frontend
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- Framer Motion (анимации)
- React Hook Form
- React Hot Toast

### Backend
- Next.js API Routes
- Prisma ORM
- PostgreSQL
- JWT Authentication
- Nodemailer

### DevOps
- Docker (опционально)
- Vercel (деплой)
- GitHub Actions (CI/CD)

## 📁 Структура проекта

```
noir-portfolio/
├── src/
│   ├── app/                 # Next.js App Router
│   │   ├── admin/          # Админ-панель
│   │   ├── api/            # API роуты
│   │   ├── blog/           # Блог страницы
│   │   ├── projects/       # Проекты
│   │   └── ...             # Другие страницы
│   ├── components/         # React компоненты
│   │   ├── common/         # Общие компоненты
│   │   ├── layout/         # Layout компоненты
│   │   ├── sections/       # Секции страниц
│   │   ├── ui/            # UI компоненты
│   │   └── admin/         # Админ компоненты
│   ├── lib/               # Утилиты и конфигурации
│   │   ├── prisma/        # Prisma клиент
│   │   ├── auth/          # Аутентификация
│   │   ├── email/         # Email сервис
│   │   └── upload/        # Загрузка файлов
│   └── types/             # TypeScript типы
├── prisma/
│   └── schema.prisma      # Схема базы данных
├── public/                # Статические файлы
└── package.json
```

## 🚀 Быстрый старт

### 1. Клонирование и установка

```bash
git clone <repository-url>
cd noir-portfolio
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
# Database
DATABASE_URL="postgresql://username:password@localhost:5432/noir_portfolio"

# JWT Secret (generate a secure random string)
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="7d"

# Email Configuration (optional)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
SMTP_FROM="noreply@noir.com"

# Admin Configuration
ADMIN_EMAIL="admin@noir.com"
ADMIN_PASSWORD="change-this-password"
```

### 3. Настройка базы данных

Убедитесь, что у вас установлен PostgreSQL и создайте базу данных:

```bash
# Создание базы данных (если нужно)
createdb noir_portfolio

# Генерация Prisma клиента
npx prisma generate

# Применение схемы к базе данных
npx prisma db push

# Заполнение начальными данными
npm run db:seed

# Или полная перезагрузка БД с данными
npm run db:reset

# Открытие Prisma Studio для просмотра данных
npx prisma studio
```

### 4. Запуск приложения

```bash
# Режим разработки
npm run dev

# Сборка для продакшена
npm run build
npm start
```

### 5. Первый вход в админ-панель

1. Откройте `http://localhost:3000/admin/login`
2. Система обнаружит, что это первый запуск
3. Создайте учетную запись администратора
4. Войдите в систему

Приложение будет доступно на `http://localhost:3000`

### 📧 Настройка Email (опционально)

Для отправки уведомлений о новых сообщениях настройте SMTP:

1. Создайте приложение в Gmail/Google
2. Получите app password
3. Обновите переменные окружения

Без настройки email система будет работать, но уведомления не будут отправляться.

## 📊 API Endpoints

### Проекты
- `GET /api/projects` - Получить все проекты
- `POST /api/projects` - Создать проект
- `GET /api/projects/[id]` - Получить проект по ID
- `PUT /api/projects/[id]` - Обновить проект
- `DELETE /api/projects/[id]` - Удалить проект

### Контакты
- `POST /api/contact` - Отправить сообщение
- `GET /api/contact` - Получить все сообщения (админ)

### Аутентификация
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/logout` - Выход из системы
- `GET /api/auth/me` - Получить текущего пользователя

## 👨‍💻 Админ-панель

Админ-панель доступна по адресу `/admin` после аутентификации.

### Возможности:
- Управление проектами (CRUD)
- Управление постами блога
- Просмотр контактных сообщений
- Настройки сайта
- Аналитика и статистика

## 🎨 Кастомизация

### Цветовая схема
Измените переменные в `tailwind.config.ts`:

```typescript
colors: {
  primary: '#ee4818',    // Основной цвет
  background: '#000',   // Фон
  text: '#d5d5d5',      // Текст
}
```

### Контент
- Измените контент в компонентах секций
- Обновите мета-теги в `layout.tsx`
- Настройте навигацию в `Header.tsx`

## 🚀 Деплой

### Vercel (рекомендуется)
```bash
npm i -g vercel
vercel --prod
```

### Docker
```bash
docker build -t noir-portfolio .
docker run -p 3000:3000 noir-portfolio
```

## 📝 Скрипты

```bash
npm run dev          # Запуск в режиме разработки
npm run build        # Сборка для продакшена
npm run start        # Запуск продакшен версии
npm run lint         # Проверка кода
npm run db:generate  # Генерация Prisma клиента
npm run db:push      # Применение изменений БД
npm run db:studio    # Открыть Prisma Studio
```

## 🔒 Безопасность

### Важные замечания:

1. **JWT Secret**: Всегда используйте уникальный и сложный JWT_SECRET в продакшене
2. **Пароли**: Система автоматически хеширует пароли с bcrypt
3. **SQL Injection**: Prisma ORM защищает от SQL инъекций
4. **XSS**: Next.js автоматически экранирует контент
5. **Rate Limiting**: Рассмотрите добавление rate limiting для API роутов

### Переменные окружения для продакшена:

```env
NODE_ENV="production"
JWT_SECRET="your-production-jwt-secret-here"
DATABASE_URL="your-production-database-url"
```

## 🚀 Продакшен деплой

### На Vercel (рекомендуется):

1. Подключите репозиторий к Vercel
2. Добавьте переменные окружения в настройках проекта
3. Настройте PostgreSQL базу данных (Railway, PlanetScale, или Supabase)
4. Деплой!

### С переменными окружения:

```bash
# Установка переменных в Vercel
vercel env add JWT_SECRET
vercel env add DATABASE_URL
vercel env add SMTP_USER
vercel env add SMTP_PASS
```

### Docker (для локальной разработки):

```bash
# Запуск с Docker Compose
docker-compose up -d

# Приложение будет доступно на http://localhost:3000
# База данных на localhost:5432
```

### Альтернативы:
- **Railway**: Полный стек (DB + App)
- **Render**: Хороший выбор для Next.js
- **DigitalOcean App Platform**: Гибкая настройка

## 🛠 Расширение функционала

### Реализованные возможности:

1. **📝 Rich Text Editor**: Полнофункциональный WYSIWYG редактор на базе TipTap для создания красивых описаний проектов с поддержкой:
   - Форматирования текста (жирный, курсив, зачеркнутый)
   - Заголовков и списков
   - Ссылок и изображений
   - HTML рендеринга с санитизацией

### Возможные дополнения:

1. **File Upload**: Интеграция с Cloudinary или AWS S3
2. **Analytics**: Google Analytics, Mixpanel
3. **SEO**: Автоматическая генерация sitemap.xml
4. **Multi-language**: i18n поддержка
5. **API Rate Limiting**: Защита от спама
6. **Backup**: Автоматическое резервное копирование БД
7. **Monitoring**: Sentry для отслеживания ошибок

### Добавление новых функций:

```typescript
// Пример: Добавление тегов к проектам
// 1. Обновить Prisma схему
// 2. Добавить API роуты
// 3. Обновить компоненты
// 4. Обновить типы TypeScript
```

## 🤝 Вклад в проект

1. Fork проект
2. Создайте ветку для вашей фичи (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 🎯 Тестирование в браузере

### ⚡ Быстрый запуск (Рекомендуется для первого тестирования)

#### Windows:
1. **Дважды кликните** на файл `start.bat` в корне проекта
2. Или откройте командную строку и выполните: `start.bat`

#### macOS/Linux:
1. **Сделайте скрипт исполняемым:**
   ```bash
   chmod +x setup.sh
   ```

2. **Запустите настройку:**
   ```bash
   ./setup.sh
   ```

**Что делает автоматическая настройка:**
- ✅ Проверяет Node.js 18+
- 📦 Устанавливает все зависимости
- 📝 Создает `.env.local` с настройками для разработки
- 🗄️ Настраивает SQLite базу данных
- 🌱 Загружает демо-данные (проекты, услуги, отзывы)
- 🚀 Запускает приложение на `http://localhost:3000`

### 📱 Что можно протестировать:

#### 🌐 Публичная часть (`http://localhost:3000`):
- **Главная страница** - анимированный hero, секции услуг, проектов, отзывов
- **Проекты** (`/projects`) - галерея с фильтрами, детальные страницы с Rich Text
- **Контактная форма** - отправка сообщений (работает без email)
- **Responsive дизайн** - попробуйте на разных размерах экрана

#### 👨‍💻 Админ-панель (`http://localhost:3000/admin/login`):
- **Вход:** `admin@noir.com` / `admin123`
- **Dashboard** - статистика проектов и сообщений
- **Проекты** - CRUD операции с Rich Text Editor
- **Сообщения** - просмотр отправленных форм

#### 📝 Rich Text Editor тестирование:
1. В админке → Projects → Add Project
2. Попробуйте все инструменты: **Ж**, *К*, ~~Зачерк~~, заголовки, списки
3. Вставьте ссылку или изображение
4. Сохраните и посмотрите результат на публичной странице

### 🛠 Ручная настройка (если автоматическая не работает):

#### 1. Проверьте требования:
```bash
node --version  # Должен быть 18+
npm --version   # Должен быть установлен
```

#### 2. Установка зависимостей:
```bash
npm install
```

#### 3. Переменные окружения:
```bash
# Создайте .env.local:
DATABASE_URL="file:./dev.db"
JWT_SECRET="development-jwt-secret-key-replace-in-production"
ADMIN_EMAIL="admin@noir.com"
ADMIN_PASSWORD="admin123"
NEXTAUTH_URL="http://localhost:3000"
```

#### 4. База данных:
```bash
npx prisma generate
npx prisma db push --force-reset
npm run db:seed
```

#### 5. Запуск:
```bash
npm run dev
```

### 🔧 Устранение неполадок:

#### Порт 3000 занят:
```bash
# Запустите на другом порту:
npm run dev -- -p 3001
# Затем откройте http://localhost:3001
```

#### Проблемы с зависимостями:
```bash
# Очистите кеш и переустановите:
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

#### Ошибки базы данных:
```bash
# Пересоздайте базу:
npm run db:reset
```

#### Проверьте логи:
- В терминале где запущен `npm run dev`
- Ищите ошибки в красном цвете
- Проверьте `.env.local` на правильность

### 🎨 Что можно увидеть:

#### 🎭 Визуальный дизайн:
- **Темная тема Noir** с оранжевыми акцентами (#ee4818)
- **Современная типографика** (Inter + Plus Jakarta Sans)
- **Плавные анимации** Framer Motion
- **Magic Cursor** - курсор следует за движениями мыши

#### ⚡ Функциональность:
- **JWT Аутентификация** - защищенные роуты админки
- **Rich Text Editor** - полноценное форматирование текста
- **API** - REST endpoints для всех операций
- **База данных** - Prisma ORM с SQLite/PostgreSQL

#### 📱 Адаптивность:
- **Mobile-first** подход
- **Responsive** дизайн
- **Touch-friendly** интерфейсы

### 🚀 Для продакшена:

Когда будете готовы к деплою:

1. **База данных:** Замените SQLite на PostgreSQL
2. **JWT Secret:** Сгенерируйте новый секрет
3. **Email:** Настройте SMTP для уведомлений
4. **Домен:** Обновите NEXTAUTH_URL
5. **SSL:** Включите HTTPS

---

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 📞 Поддержка

Если возникнут проблемы при запуске:
1. Проверьте логи в терминале
2. Убедитесь что Node.js 18+ установлен
3. Попробуйте `npm cache clean --force && npm install`
4. Создайте issue в репозитории

---

## 🚀 Развертывание в продакшн

Для развертывания на домашнем сервере с автоматическими обновлениями через GitHub:

### 📚 Документация по развертыванию:
- **[DEPLOYMENT-GUIDE.md](DEPLOYMENT-GUIDE.md)** - Полное руководство по настройке CI/CD
- **[DEPLOY-CHECKLIST.md](DEPLOY-CHECKLIST.md)** - Чек-лист для быстрой проверки

### 🛠️ Быстрый старт развертывания:
1. **Сервер:** `sudo ./server-setup.sh` (один раз на сервере)
2. **GitHub:** Настроить секреты (SERVER_HOST, SERVER_USER, SERVER_SSH_KEY)
3. **Развертывание:** `git push origin main` → автоматическое обновление через GitHub Actions

### 📁 Ключевые файлы для развертывания:
- `docker-compose.prod.yml` - Продакшн конфигурация с PostgreSQL + Nginx
- `.github/workflows/docker-deploy.yml` - CI/CD pipeline
- `nginx.conf` - Веб-сервер с SSL и кешированием
- `deploy.sh` - Скрипт развертывания на сервере
- `server-setup.sh` - Начальная настройка сервера

**🎉 Наслаждайтесь Noir Portfolio - полной системой управления портфолио с Rich Text Editor и автоматическим CI/CD!**
